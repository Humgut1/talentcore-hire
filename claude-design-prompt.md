# Claude Design 프롬프트 — TalentCore ATS 인터뷰 스케줄링 모듈

> 아래 `---` 사이 전체를 복사해서 claude.ai/design 에 붙여넣으세요.

---

# TalentCore ATS — 인터뷰 스케줄링 자동화 모듈 UI 설계

## 0. 한 문장 요약

기존 HR 플랫폼 **TalentCore**에 얹히는 **채용(ATS) 모듈**을 디자인해줘. 핵심은 인터뷰 일정 조율 자동화이고, **HR Coordinator · Recruiter · Hiring Manager 세 역할이 각자 처음 로그인해도 매뉴얼 없이 자기가 뭘 해야 하는지 즉시 아는 UI**를 만드는 게 이 제품의 유일한 차별점이야. 이 목표를 다른 모든 것보다 우선해줘.

---

## 1. 제품 배경

TalentCore는 이미 운영 중인 국내 HR SaaS다 (Flask + Jinja2 + 바닐라 CSS/JS, 근로기준법 준수 자동화, 조직도·근태·급여·성과·보상 모듈 보유). 여기에 채용 모듈을 붙이는데, **별도 제품처럼 보이면 안 되고 같은 플랫폼의 한 탭처럼** 느껴져야 해.

### 해결하려는 진짜 문제

채용 코디네이터 업무의 가장 큰 비중은 "인터뷰 일정 조율"이라는 반복 행정이다. 구체적으로:

1. **캘린더 공백 ≠ 실제 가능** — AI가 캘린더만 보고 일정을 확정하면 재조율이 반복된다
2. **2차 면접은 면접관 2인의 2시간 연속 교집합**을 찾아야 해서 난이도가 높다
3. **C레벨 면접관은 EA(비서)가 알림을 필터링**해서 자동 알림이 무의미하다
4. **킥오프·디브리프 미팅** 조율이 완전 수동으로 남아 있다
5. 전형별 소요시간·전환율 데이터가 흩어져 분석이 안 된다

### 설계 철학 (반드시 UI에 드러날 것)

> **AI가 모든 걸 자동화하는 게 아니라, HR Coordinator가 덜 소모적으로 일하도록 보조한다. 패턴이 있으면 AI가 처리하고, 예외는 사람에게 넘긴다.**

| 구분 | 담당 |
|---|---|
| AI | 슬롯 탐색, 알림 발송, 리마인더, 상태 업데이트, 데이터 기록 |
| 사람 | 예외 협상, 임원 조율, 최종 판단, 관계 관리 |

이 분담이 **화면에서 눈에 보여야 해.** "AI가 처리 중인 것"과 "내가 손대야 하는 것"이 시각적으로 완전히 구분되어야 한다.

---

## 2. 세 명의 사용자 — 각자 완전히 다른 제품처럼 느껴야 함

### ① HR Coordinator (RC) — 매일, 하루 종일 사용

- 실질적인 헤비유저. 이 사람의 화면이 제품의 기준점.
- 하는 일: **에스컬레이션 처리**. 정상 진행 중인 건은 볼 필요가 없고, **막힌 것만** 봐야 한다.
- 알림을 받았을 때 **상황을 처음부터 재파악하는 구조는 절대 안 됨.** 알림 카드 하나에 포지션·후보자·면접관·문제 원인·추천 액션이 전부 들어 있어야 하고, 버튼 한 번으로 조율을 시작할 수 있어야 한다.
- EA 조율 대상 임원이 낀 건은 처음부터 자동화에서 빠져 이 사람에게 온다.

### ② Recruiter — 하루 몇 번, 포지션 단위로 사고

- 자기가 담당한 포지션들의 **진행 상황과 후보자 흐름**을 본다.
- 관심사: 어느 후보자가 어느 단계에 있고, 누가 지금 대기 중이고, 오퍼까지 얼마나 남았는가.
- 데이터 Export와 대시보드를 실제로 쓰는 사람.

### ③ Hiring Manager (HM) — 주 1~2회, 학습 의지 0

- **가장 중요한 제약**: 이 사람은 시스템을 배울 생각이 없다. 로그인조차 귀찮아한다.
- HM이 해야 하는 일은 딱 4가지: ①서류 Pass/Fail 클릭 ②면접 슬롯 클릭 선택 ③킥오프 참석자(차상위 리더·협업 리더) 지정 ④면접 결과 입력.
- 이 4가지가 **텍스트 입력 없이 클릭만으로** 끝나야 한다.
- HM은 Slack/이메일 알림 안에서 바로 처리하는 게 기본 동선이고, 웹에 들어와도 홈 화면에 **"지금 당신 차례인 것"만** 보여야 한다. 메뉴 탐색을 시키면 안 된다.

---

## 3. 반드시 구현해야 할 UX 논제 (이게 차별점이다)

추상적인 "직관적으로"가 아니라, 아래 6개를 구체적인 화면 장치로 만들어줘.

### 논제 1 — "지금 당신 차례" 단일 인박스

세 역할 모두 로그인 직후 첫 화면이 **내가 처리해야 할 것들의 큐**다. 대시보드 지표가 아니라 액션 큐가 최상단. 각 항목은 카드 한 장 안에서 처리가 끝나거나, 한 번의 클릭으로 처리 화면에 도달해야 한다. **큐가 비면 "지금 처리할 일이 없습니다"가 당당하게 보이는 빈 상태**를 디자인해줘 — 이게 이 제품의 자랑거리다.

### 논제 2 — 후보자 1명 = 수평 타임라인 하나 (제품의 시그니처 화면)

`지원 → 서류 → 1차 인터뷰 → 킥오프 → 2차 인터뷰 → 디브리프 → 오퍼 → 입사`

이 전체를 **하나의 수평 타임라인 컴포넌트**로 표현해줘. 각 노드는 4가지 상태 중 하나:

- **완료** — 날짜/시각 + 담당자 표시
- **AI 진행 중** — "슬롯 3개 발송함, 이OO 팀장 응답 대기 · 14시간 경과" 처럼 지금 무엇을 기다리는지 명시
- **사람 대기(에스컬레이션)** — 시각적으로 강하게 튀어야 함. 원인 + 액션 버튼
- **미도달** — 흐리게

이 타임라인이 후보자 상세, 포지션 보드, 코디네이터 인박스 등 여러 곳에서 크기만 달리해 재사용되는 **하나의 디자인 언어**가 되게 해줘.

### 논제 3 — 캘린더 교집합 시각화

여러 면접관의 캘린더를 겹쳐 보고 공통 가능 슬롯을 찾는 화면. 반드시:

- **세로축(행) = 시간, 가로축(열) = 요일** (구글 캘린더 방식. 축 반대로 하지 말 것)
- 면접관별 바쁨 블록을 **반투명 레이어로 오버레이**해서, 겹치지 않는 흰 구간이 곧 "가능 슬롯"으로 읽히게
- 슬롯 블록은 **1시간 단위 큰 셀 → 클릭하면 팝업에서 분 단위 조정**
- AI가 추천한 슬롯 3개는 별도로 강조
- 2차 인터뷰는 **2시간 연속 블록**이 필요하다는 걸 시각적으로 표현 (연속 두 칸이 함께 하이라이트)
- "교집합 슬롯 0개"인 실패 상태도 반드시 디자인 — 이때가 코디네이터가 개입하는 순간이다

### 논제 4 — 면접관용 알림 UI (제품 밖에서 벌어지는 화면)

HM/면접관이 실제로 마주하는 접점 3종을 모두 디자인해줘:

- **Slack 메시지 블록** — 후보자 정보 + 슬롯 3개 버튼 + [모두 불가] 버튼
- **이메일 템플릿** — 같은 내용의 HTML 이메일
- **모바일 웹 슬롯 선택 페이지** — 로그인 없이 서명된 링크로 진입, 엄지로 3초 안에 완료

이게 실제로 제품 성패를 가른다. 대충 만들지 말고 제대로 디자인해줘.

### 논제 5 — "AI가 지금 뭘 하고 있는지" 투명하게

자동화 제품은 **불투명하면 신뢰받지 못한다.** 코디네이터가 언제든 "지금 AI가 처리 중인 건 목록"을 볼 수 있어야 하고, 각 건마다 `무엇을 · 누구에게 · 언제 보냈고 · 언제까지 기다리는지`가 보여야 한다. 필요하면 사람이 중간에 가로채서 수동 전환할 수 있어야 한다.

### 논제 6 — 학습 없는 학습성

- 각 화면 첫 진입 시 **"이 화면에서 하는 일" 한 줄 설명**이 상단에 자연스럽게 있을 것 (모달 튜토리얼 금지)
- **빈 상태(empty state)가 곧 사용 설명서** 역할을 하도록 디자인
- 용어는 전부 한국어. 단, 업계 표준어(킥오프, 디브리프, 오퍼, 리크루터)는 그대로 쓴다

---

## 4. 하나의 플로우 · 데이터 연동 (TalentCore 통합 원칙)

**별도 앱이 아니라 TalentCore의 한 모듈**이다. 아래 연결이 UI에서 드러나야 해:

| 연결 지점 | 동작 |
|---|---|
| 면접관 마스터 | 별도 등록 없음. **TalentCore 직원 레코드의 확장 속성**으로 설정 (직원 상세에 '면접관 설정' 섹션) |
| 조직 정보 | 부문 / 본부 / 부서 트리를 TalentCore 조직도에서 그대로 상속 |
| 채용요청서 승인 | TalentCore의 **기존 결재선 프레임워크 재사용** (부서 승인 → HR 승인) |
| 결재 대기함 | ATS 항목(참석자 지정 요청, 오퍼 승인 등)이 **TalentCore 통합 결재 대기함에 함께** 뜬다 |
| 알림 | TalentCore 인박스에 채용 알림이 섞여서 표시 |
| 오퍼 수락 이후 | **TalentCore '입사 예정자' 레코드로 자동 승계** → 온보딩 모듈로 인계 |
| 권한 | TalentCore RBAC(관리자/매니저/직원) 위에 ATS 역할(리크루터·코디네이터·면접관·HM)이 얹히는 구조 |

### 권한 매트릭스 (화면에 반영할 것)

| 역할 | 후보자 데이터 | 대시보드 | 스케줄링 | 오퍼 |
|---|---|---|---|---|
| HR Manager | 전체 | 전사 전체 | 전체 개입 | 전체 |
| 리크루터 | 담당 포지션 | 담당 포지션 | 담당 포지션 | 발송·기록 |
| HR Coordinator | 조율 대상 전체 | 스케줄링 지표 | **에스컬레이션 처리 주체** | 조회만 |
| Hiring Manager | **본인 부서 포지션·후보자만 (개인정보 제한)** | 본인 부서 | 슬롯 선택·참석자 지정 | 불가 |
| 면접관 | **배정된 후보자만** | 불가 | 슬롯 선택만 | 불가 |
| 경영진 | 불가 | 전사 집계 요약만 | 불가 | 불가 |

권한에 따라 화면이 "회색 처리"되는 게 아니라 **애초에 다른 화면이 뜨는 방식**으로 설계해줘.

---

## 5. 디자인해야 할 화면 목록

우선순위 순. 1~6번이 핵심이고, 이것부터 만들어줘.

### ★ 1. HR Coordinator 홈 — 에스컬레이션 인박스

정상 건은 숨기고 막힌 것만. 에스컬레이션 트리거 종류와 우선순위:

| 트리거 | 조건 | 우선순위 |
|---|---|---|
| EA 조율 대상 포함 | 임원 면접관 플래그 감지 즉시 | 높음 |
| 교집합 슬롯 없음 | 2차·킥오프·디브리프 공통 슬롯 0개 | 높음 |
| 킥오프 참석자 미지정 | HM이 48시간 내 미지정 | 높음 |
| 면접관 미응답 | 응답 제한시간 + 24시간 경과 | 중간 |
| 전체 슬롯 거절 | 제안 슬롯 모두 불가 선택 | 중간 |
| 디브리프 추가 후보자 | 확정된 디브리프 후 2차 완료자 추가 발생 | 중간 |
| 일정 변경 요청 | 확정 후 변경 요청 | 중간 |
| 후보자 미응답 | 48시간 경과 | 낮음 |

각 카드에 반드시 포함: **포지션(부문>본부>부서) · 후보자명과 현재 단계 · 관련 면접관 · 문제 내용 · [수동 조율 시작] 액션 버튼**

### ★ 2. Recruiter 홈 — 포지션 보드

담당 포지션 카드들. 각 포지션 안에 후보자별 수평 타임라인(논제 2)이 압축 표시. 어디가 막혔는지 한눈에.

### ★ 3. Hiring Manager 홈 — 극단적으로 단순한 "내 차례" 화면

카드 3~5장이 전부. 서류 검토 대기 / 슬롯 선택 요청 / 참석자 지정 요청 / 면접 결과 입력. 그 외 아무것도 보이지 않아야 함.

### ★ 4. 후보자 상세 — 전형 타임라인 + 이벤트 로그

수평 타임라인(논제 2) 풀사이즈 + 모든 자동 기록 이벤트의 타임스탬프 로그 + 이력서/피드백 링크.

### ★ 5. 스케줄링 조율 화면 — 캘린더 교집합 (논제 3)

1차 인터뷰 / 2차 인터뷰(2시간 연속) / 킥오프(30분) / 디브리프(30분) 네 가지 케이스를 같은 컴포넌트로 커버. 슬롯 있음 / 슬롯 없음 두 상태 모두.

### ★ 6. 면접관 접점 3종 (논제 4)

Slack 블록 · 이메일 · 모바일 슬롯 선택 페이지.

### 7. 킥오프 참석자 지정 화면 (HM용)

차상위 리더 · 협업 리더를 TalentCore 조직도 검색으로 지정. 드롭다운 2개면 끝나야 함.

### 8. 면접관 프로필 설정 (직원 상세 안의 섹션)

- 조율 방식: 직접 연락 / **EA를 통한 조율** (EA 설정 시 자동화 제외됨을 명확히 경고)
- 알림 채널: Slack / 이메일 / 둘 다
- 응답 제한 시간: 12h / 24h / 48h
- 역할 태그: 하이어링 매니저 / 인터뷰어 / 리크루터 / 코디네이터

### 9. 채용 대시보드 (권한별 스코프 적용)

- 퍼널 전환율 (지원→서류→1차→2차→오퍼→수락, 각 단계 %)
- 소요시간: 단계별 평균 소요일, Time-to-Fill, Time-to-Hire, **스케줄링 조율 소요시간**, **HR 수동 개입 비율**
- 출처별 효율: 채널별 지원 수·최종 합격률·Time-to-Hire, 레퍼럴 추천인 순위
- 부서·리크루터·면접관별 현황 (면접관별 평균 응답시간 포함)
- 필터: 기간 / 부문·본부·부서 / 포지션 / 리크루터 / 고용형태

> **스케줄링 조율 소요시간**과 **HR 수동 개입 비율**은 이 제품의 존재 이유를 증명하는 지표다. 대시보드에서 눈에 띄게 배치해줘.

### 10. 데이터 Export

기간·부문/본부/부서·포지션·리크루터·최종결과 필터 → .xlsx 다운로드. 후보자 1명 × 포지션 1건 = 1행. 40여 개 컬럼(지원일시, 각 전형 일시/면접관/결과, 킥오프·디브리프 일정과 참석자, 오퍼 발송/결과/거절사유, 최종 결과, 입사일, 소요일 자동 계산 6종, 출처 6종).

### 11. Google Calendar 인비테이션 동기화 상태

일정 확정 즉시 코디네이터 계정을 오너로 인비 자동 생성. 1차·킥오프·디브리프는 인비 1개, **2차 인터뷰는 면접관별로 개별 인비 2개**(면접관1이 면접관2의 시간대를 보고 혼란스럽지 않도록). 코디네이터가 구글 캘린더에서 직접 수정한 경우 **'수동 수정' 로그**가 ATS에 남는 화면.

### 12. 포지션 설정 — 전형 단계별 대면/화상 방식

화상 선택 시 Google Meet / Zoom / Teams 링크 자동 생성, 대면 선택 시 장소 입력 필드로 전환.

---

## 6. 디자인 시스템 — TalentCore 기존 토큰 준수

기존 제품과 이질감이 없어야 한다. **Editorial Soft-Minimalism**이 기존 컨셉이야.

```
/* Surfaces — 종이를 겹친 듯한 톤 레이어 */
--surface: #f6f9ff;
--surface-container-low: #edf4fe;
--surface-container: #e7eff9;
--surface-container-high: #e1e9f2;
--surface-container-highest: #d9e2ed;
--surface-container-lowest: #ffffff;

/* Text */
--on-surface: #151c23;
--on-surface-variant: #4a5568;
--on-surface-muted: #6b7a8c;

/* Primary */
--primary: #2b5bff;
--primary-container: #6b8eff;
--on-primary: #ffffff;

/* Tonal containers */
--secondary-container: #dfe7ff;  --on-secondary-container: #15306b;
--tertiary-container: #e5efd7;   --on-tertiary-container: #1f3b10;
--warn-container: #fce8cf;       --on-warn-container: #4a2f00;

/* Radii */  xs .5rem / sm .75rem / md 1.5rem / lg 2rem / xl 3rem / full
/* Shadow */ 0 24px 60px rgba(21,28,35,.04), 0 2px 8px rgba(21,28,35,.02)

폰트: 'Plus Jakarta Sans', system-ui, sans-serif
```

### 절대 규칙 (기존 시스템에서 이미 확정된 것)

1. **`border: 1px solid` 사용 금지** — 요소 구분은 배경색 톤 차이로만
2. **텍스트에 `#000` / `black` 금지** — 항상 `var(--on-surface)`
3. **카드 radius 24px 이상**
4. **주요 CTA는 그라디언트**

### 이 모듈에서 추가로 필요한 색 (직접 정의해줘)

기존 팔레트가 파란색 단일 계열이라 **상태 구분이 부족하다.** 아래 4개 상태를 서로 확실히 구분되면서 기존 톤과 어울리는 색으로 정의해줘:

- `AI 진행 중` — 차분함. 사람이 볼 필요 없다는 신호
- `사람 대기 / 에스컬레이션` — 강하게 튀어야 함. 이게 이 제품에서 가장 중요한 색
- `완료` — 조용한 확정감
- `지연 / 시간 초과` — 경고

`--primary`(#2b5bff)는 이미 액션 버튼에 쓰이므로, **에스컬레이션 색은 primary와 절대 헷갈리면 안 된다.**

---

## 7. 기타 제약

- **한국어 UI 기본.** 외국인 후보자 대응을 위해 영어 알림 템플릿도 있지만, 관리 화면은 한국어.
- **데스크톱 우선** — 단, 면접관 접점(슬롯 선택, 서류 Pass/Fail, 결과 입력)은 **모바일 우선**으로 설계.
- 타임존은 면접관·후보자 각자 기준으로 표시(예: "14:00 KST · 지원자 현지 15:00").
- 모든 알림·응답·에스컬레이션·수동 조율 이력이 감사 로그에 남는다 — 이걸 볼 수 있는 화면도 어딘가 필요.
- 후보자 개인정보는 HM에게 제한적으로 노출된다. 마스킹 처리 패턴을 정해줘.
- 다크 모드는 필수는 아니지만, 지원한다면 라이트 모드와 동등한 완성도로.

---

## 8. 산출물 요청

1. **먼저 디자인 방향을 짧게 제안**해줘 — 특히 §6에서 요청한 4개 상태 색과, 논제 2의 수평 타임라인 컴포넌트를 어떻게 풀 건지. 여기서 합의하고 진행하고 싶어.
2. 그다음 **★ 표시된 1~6번 화면을 HTML/CSS/JS 프로토타입**으로 만들어줘. 실제 인터랙션(슬롯 클릭, 팝업, 상태 전환)이 동작하면 좋겠어.
3. 나머지 화면은 그 뒤에.
4. 목업 데이터는 한국 IT 회사 맥락으로 현실감 있게 (포지션: 백엔드 개발자(시니어) / 조직: 개발부문 > 플랫폼본부 > 서버팀 등).

**가장 중요한 판단 기준을 다시 강조할게** — 세 역할 중 누구라도 이 화면을 처음 보고 "뭘 눌러야 하지?"라고 3초 이상 고민한다면 그 디자인은 실패한 거야. 기능을 다 담는 것보다 그게 우선이야.

---
